energyops:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres          # IMPORTANT: depuis un container, on vise le service docker "postgres"
      user: energy
      password: energy
      port: 5432
      dbname: energyops
      schema: analytics
      threads: 4
